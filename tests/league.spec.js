const League = require('../src/league');

describe('League object', function () {
  it('should not return undefined', function () {
    // Arrange
    //const expected = undefined;

    //// Act
    //const actual = League();

    //// Assert
    //expect(actual).not.toBe(expected);
  });
});
